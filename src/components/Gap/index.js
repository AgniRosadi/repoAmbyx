import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const Gap = ({ height, width }) => {
    return (
        <View style={{ heigh: height, width: width }} />
    )
}

export default Gap

const styles = StyleSheet.create({})
