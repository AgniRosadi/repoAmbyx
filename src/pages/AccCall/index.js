import React from 'react'
import { StyleSheet, Text, View } from 'react-native'
import { HeaderClose, Tips, Body, ButtomPesan, ButtomLatter } from '../../components'

const AccCall = () => {
    return (
        <View>
            <HeaderClose />
            <Tips />
            <Body />
            <ButtomPesan />
            <ButtomLatter />
        </View>
    )
}

export default AccCall

const styles = StyleSheet.create({})
